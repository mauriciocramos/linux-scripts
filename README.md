# linux-scripts
Miscellaneuous Ubuntu scripts for AIO cooler, CONDA environments, etc

Nvidia monitoring:

```
watch -n1 nvidia-smi
nvidia-smi-pmon.sh
nvidia-log-compute-apps.sh
nvidia-log-gpu.sh 
```